extends subpage

block subcontent
  h1.font-bold.text-3xl Classes
  div.flex.flex-col.pt-6.gap-4
    each cl in classes
      a.shadow-md.bg-slate-500.text-white.rounded-xl.px-5.py-2(href="/classes/" + cl.codigo class="hover:bg-slate-600")
        p= `[${cl.codigo}] ${cl.titulo}`